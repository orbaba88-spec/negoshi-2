import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  define: {
    // These match the global variables used in the Canvas environment
    // In production, these should be provided via environment variables
    __firebase_config: process.env.VITE_FIREBASE_CONFIG || '{}',
    __app_id: JSON.stringify(process.env.VITE_APP_ID || 'negoshi-v2'),
    __initial_auth_token: JSON.stringify(process.env.VITE_AUTH_TOKEN || null)
  }
});
